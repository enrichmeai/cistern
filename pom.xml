<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>com.enrichmeai.cistern</groupId>
  <artifactId>cistern-parent</artifactId>
  <version>0.1.0-SNAPSHOT</version>
  <packaging>pom</packaging>

  <name>Cistern</name>
  <description>Open, self-hostable Solid pod server for the AI era — JVM-native, MCP-fronted, conformance-first.</description>
  <url>https://enrichmeai.com</url>

  <licenses>
    <license>
      <name>Apache License, Version 2.0</name>
      <url>https://www.apache.org/licenses/LICENSE-2.0.txt</url>
    </license>
  </licenses>

  <organization>
    <name>Good Shepherd Software Consultancy Ltd, trading as EnrichMeAI</name>
    <url>https://enrichmeai.com</url>
  </organization>

  <modules>
    <module>cistern-core</module>
    <module>cistern-auth</module>
    <module>cistern-wac</module>
    <module>cistern-storage-file</module>
    <module>cistern-webflux</module>
    <module>cistern-mcp</module>
    <module>cistern-spring-boot-starter</module>
    <module>cistern-app</module>
  </modules>

  <properties>
    <maven.compiler.release>21</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

    <!-- Versions audited against Maven Central on 2026-07-20 (#58). Latest stable of
         each line; milestones and release candidates are never selected.
         Spring Boot 4.1.0 (2026-06-10) — moved off the 3.5 line, whose OSS support
         ended 2026-06-30 at 3.5.16. Ships Spring Framework 7; baseline Java 17.
         Jena 6.1.0 (2026-05-03) — baseline Java 21, which is our compiler release.
         titanium-json-ld stays 1.7.0: 2.0.0-M2 is a milestone, and 1.7.0 is the
         version jena-arq 6.1.0 itself declares. nimbus-jose-jwt 10.9.1, mcp 2.0.0
         and surefire 3.5.6 are already the latest stable releases.
         junit-jupiter and reactor-test versions come from the imported
         spring-boot-dependencies BOM. -->
    <spring-boot.version>4.1.0</spring-boot.version>
    <jena.version>6.1.0</jena.version>
    <nimbus-jose-jwt.version>10.9.1</nimbus-jose-jwt.version>
    <titanium-json-ld.version>1.7.0</titanium-json-ld.version>
    <mcp-sdk.version>2.0.0</mcp-sdk.version>
  </properties>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-dependencies</artifactId>
        <version>${spring-boot.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <dependency>
        <groupId>org.apache.jena</groupId>
        <artifactId>jena-arq</artifactId>
        <version>${jena.version}</version>
      </dependency>
      <dependency>
        <groupId>com.nimbusds</groupId>
        <artifactId>nimbus-jose-jwt</artifactId>
        <version>${nimbus-jose-jwt.version}</version>
      </dependency>
      <dependency>
        <groupId>com.apicatalog</groupId>
        <artifactId>titanium-json-ld</artifactId>
        <version>${titanium-json-ld.version}</version>
      </dependency>
      <dependency>
        <groupId>io.modelcontextprotocol.sdk</groupId>
        <artifactId>mcp</artifactId>
        <version>${mcp-sdk.version}</version>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <dependencies>
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>io.projectreactor</groupId>
      <artifactId>reactor-test</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.springframework.boot</groupId>
          <artifactId>spring-boot-maven-plugin</artifactId>
          <version>${spring-boot.version}</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-surefire-plugin</artifactId>
          <version>3.5.6</version>
        </plugin>
      </plugins>
    </pluginManagement>
  </build>
</project>
