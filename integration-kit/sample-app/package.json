{
  "name": "cistern-integration-sample-app",
  "version": "0.1.0",
  "private": true,
  "description": "The smallest application that reads a Cistern pod under a grant and is refused outside it — plain fetch, no framework, no SDK.",
  "license": "Apache-2.0",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/main.js"
  },
  "devDependencies": {
    "@types/node": "^20.17.0",
    "typescript": "^5.6.0"
  }
}
